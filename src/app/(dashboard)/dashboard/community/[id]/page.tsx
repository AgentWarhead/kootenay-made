import PlaceholderPage from '../../../PlaceholderPage';

export default function CommunityPostPage({ params }: { params: { id: string } }) {
  return <PlaceholderPage title="Community Post" emoji="💬" description="This post is loading up." />;
}
